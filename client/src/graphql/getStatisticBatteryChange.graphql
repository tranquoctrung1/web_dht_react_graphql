query GetStatisticBatteryChange($date: Date) {
    GetStatisticBatteryChange(date: $date) {
        AccreditationDocument
        DateOfChange
        DescriptionOfChange
        ExpiryDate
        Id
        Location
        Marks
        Meter
        Size
        Transmitter
    }
}
