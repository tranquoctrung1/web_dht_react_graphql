import { Outlet } from 'react-router-dom';
const Layout = () => {
    return (
        <>
            <div>adfasdfasdfasd</div>
            <div id="detail">
                <Outlet />
            </div>
        </>
    );
};

export default Layout;
